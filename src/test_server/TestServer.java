package test_server;

import talk_server.TalkServer;

public class TestServer {
    public static void main(String[] args) {
        TalkServer ts = new TalkServer();
        ts.setVisible(true);
    }
}
